{
  "name": "paulpoleon-spotify-jam-worker",
  "private": true,
  "type": "module",
  "scripts": { "dev": "wrangler dev --env dev", "deploy": "wrangler deploy --env=\"\"", "test": "node --test" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
